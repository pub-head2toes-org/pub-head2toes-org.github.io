# PWA Bandage - Update 2

This update focus in some improvements in existing recorder function and UI.

* UI: the lenght of the black keys should be shortened. Lets try with 3/4 of the current length.
* UI: show the the app name `BANDAGE` right aligned on the strip that holds selections: `VOICE`, `OCTAVE`, etc.
* Recorder is not recording the note length. For example: longer key press should be captured on a sequence as a repreated notes, so that the playback would reflect the original play. At a current the notes are recordered in only one time slot.

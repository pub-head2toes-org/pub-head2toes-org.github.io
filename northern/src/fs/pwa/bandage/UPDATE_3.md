# PWA Bandage - Update 3

This update focus in some improvements in UI/UX

# Considerations

* On edit loop:
  * Move the position left/right buttons to the left side of the tool bar
  * Increase the toolbar height
  * Make the left/right buttons wider and leave the same width between them to prevent accidental press
  * Toching the panel sets the position as expected, but it also clears the note under the table field touched
    * Change it so that only sets the current position: no clearing of the field values
  * On the main screen toolbar make wider +/- buttons for changing the octave so that can be easily touched



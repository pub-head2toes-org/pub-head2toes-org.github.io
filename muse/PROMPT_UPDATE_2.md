Develop an update for progressive web app (PWA) named `Muse` with following instructions

* Change the search logic to work as AND operation. Show only lines in the results that have matched for all of the search keys in the search key array
* Change the main page logic to remember what the search keys array is and the current position of the selection in the results list
  * Use the remembered search keys and position to restore the main page as it was when returning from other pages, like details page

